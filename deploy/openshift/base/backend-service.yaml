apiVersion: v1
kind: Service
metadata:
  name: crm-backend
  labels:
    app: crm-backend
    app.kubernetes.io/name: crm-backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: crm
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
  selector:
    app: crm-backend

